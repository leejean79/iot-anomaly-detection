# M2 逐设备 R 标定表（收尾任务书·任务一交付）

> **口径与来源**：k 全局固定 10；标定段 2022-03 整月（预热日由探针按 `isWarmup()` 自动剔除）；
> 离群率口径 = 探针 `meanOutlierRate`（每滑动步离群点占窗口点数之比、再对全部滑动步取均值）。
> 逐设备 R 由 `deploy/scripts/m2_pick_r.py` 按任务书规则**机器选取**（下表），散文观察由开发会话补充。
> 原始网格数据见 `docs/m2_probe_calib.csv`（8 设备 × R∈{0.75,1.0,1.25,1.5,1.75} × k=10）。
> **本表回传设计会话做终值确认**；被标记设备只观察成因、不自行裁决（边界要求）。

目标带 **[0.1%, 0.5%]**，带中心 **0.3%**；R 网格 **{0.75, 1.0, 1.25, 1.5, 1.75}**。

## 标定表

| 设备 | 选定 R | 该 R 下标定段离群率 | 是否标记复核 |
|---|---|---|---|
| A | 0.75 | 0.0475% | ⚠ 分辨力疑似不足、待人工复核 |
| B | 0.75 | 0.0697% | ⚠ 分辨力疑似不足、待人工复核 |
| C | 1.75 | 0.6749% | ⚠ 过度活跃、待人工复核 |
| D | 1.75 | 2.8049% | ⚠ 过度活跃、待人工复核 |
| E | 0.75 | 0.2464% | 否（落带内，最接近 0.3%） |
| F | 1.75 | 0.4987% | 否（落带内，但见下"边缘情形"） |
| G | 0.75 | 0.3692% | 否（落带内，最接近 0.3%） |
| H | 0.75 | 0.0673% | ⚠ 分辨力疑似不足、待人工复核 |

**一句话概览**：8 台里只有 **E、F、G** 三台能在给定网格内干净落入目标带；**A、B、H** 三台
无论 R 取多小都够不到 0.1%（分辨力不足），**C、D** 两台无论 R 取多大都压不到 0.5% 以下（过度活跃）。
这与 V-M2-3 的"安静档 / 活跃档"强异质性结论一脉相承——**单一目标带只对中间档设备可达**。

## 被标记设备的形态观察（只观察成因，不裁决）

**A、B、H —— 分辨力疑似不足（选 0.75，标记）**
- 形态：离群率在整个 R 网格上又平又低，即便最紧半径 R=0.75 也只有 0.048%~0.070%，且随 R 增大
  平滑降到 0.03% 上下；`fracZeroSlides`≈0.99（约 99% 的滑动步一个离群都没有）。
- 观察（不裁决）：这三台设备的标准化读数整月都挤在一个很紧、很稳的簇里，几乎每个点在任何被测半径
  内都有 ≥k=10 个邻居。点异常通道在**平稳月**里本就对它们无从下手。要把它们拉进 [0.1%,0.5%] 带，
  需要**比网格下限更紧的 R（<0.75）或更小的 k**；成因究竟是"传感器确实规矩"还是"RobustScaler 把
  它们的动态范围压得过窄"，仅凭本数据无法区分——交设计会话定夺（可选：为这三台单探 R<0.75/k<10，
  或接受它们在点通道里近乎静默、其异常留待后续 M 阶段的其它通道捕捉）。

**C、D —— 过度活跃（选 1.75，标记）**
- 形态：即便最松半径 R=1.75，离群率仍在带上方（C=0.675%，D=2.805%）；随 R 增大平滑下降、无断崖
  （D：6.07%→2.80%，C：2.00%→0.67%）。没有断崖说明**不是单个离群团**，而是分布整体更发散。
- 观察（不裁决）：这两台的标准化读数明显更"铺得开"（更分散或多峰），总有一条有意义的尾巴离簇心
  >R。要拉进带内需要**比网格上限更大的 R（>1.75）**。尤其 **D 在 R=1.75 仍有 2.8%**，值得人工
  眼看一眼：是真实的物理波动（该设备本就更动态/噪声更大），还是标准化尺度带来的假象——交设计会话
  定夺（可选：为这两台把 R 网格向上延伸，或接受它们更高的常态离群基线并据此设各自阈值）。

## 边缘情形与假设（请设计会话一并确认）

- **F 是临界通过**：F 只有 R=1.75 一点落带内（0.499%，恰在 0.5% 上沿下方一点点），R=1.5 已是
  0.671%（超带）。规则未标记它，但它实质上离"过度活跃"只差一步；若设计会话把带上沿设为**严格 <0.5%**
  或对 F 的稳健性有顾虑，F 可与 C/D 同列复核。此处如实点出，不替其裁决。
- **E、G 选 0.75 是"最接近 0.3%"的结果**，其中 G 的 0.75(0.369%) 与 1.0(0.226%) 到中心的距离几乎
  相等（0.069% vs 0.074%），近乎平手；平局按"较小 R"处置（更紧半径、更敏感）。若设计会话偏好更保守的
  1.0，G 可改 1.0，属口味级选择。
- **离群率口径**：本表用探针 `meanOutlierRate`（逐滑动步占比的均值）。若任务书所指"离群率"是
  另一口径（如"离群点总数 / 点观测总数"的聚合比），选取可能微变——请确认口径。
- **带边界含端点**：本选取把 [0.1%, 0.5%] 视为**闭区间**（F 的 0.499%、以及贴近 0.1% 的点都算带内）。

## 逐设备离群率随 R 的完整形态（k=10）

| 设备 | R=0.75 | R=1.00 | R=1.25 | R=1.50 | R=1.75 |
|---|---|---|---|---|---|
| A | 0.0475% | 0.0402% | 0.0360% | 0.0345% | 0.0343% |
| B | 0.0697% | 0.0477% | 0.0392% | 0.0289% | 0.0277% |
| C | 1.9951% | 1.3777% | 1.0334% | 0.8197% | 0.6749% |
| D | 6.0725% | 4.7184% | 3.8568% | 3.2687% | 2.8049% |
| E | 0.2464% | 0.1810% | 0.1492% | 0.1304% | 0.1187% |
| F | 2.3938% | 1.4696% | 0.9614% | 0.6712% | 0.4987% |
| G | 0.3692% | 0.2261% | 0.1660% | 0.1290% | 0.1127% |
| H | 0.0673% | 0.0453% | 0.0375% | 0.0356% | 0.0345% |

> 三条单调性在全设备成立（R↑→离群率↓），与探针的算法健康度自检一致。

## 交回设计会话的裁决点（不自行决定）

1. **A/B/H（分辨力不足）**：接受点通道对其近乎静默，还是为其单探 R<0.75 或更小 k？
2. **C/D（过度活跃）**：接受其更高常态基线（各自设阈值），还是把 R 网格向上延伸再选？D 的 2.8% 尤其待人工核。
3. **F 临界**：按现规则通过（R=1.75, 0.499%），还是并入 C/D 复核？
4. **口径与平局**：确认离群率口径 = `meanOutlierRate`；确认 G 平手取 0.75（对更小 R）的处置。

终值确认后，选定的逐设备 R 将写入 M2Job 默认配置（任务二已需按逐设备 R 跑六月段，见 `m2_df12_surge.md`）。
