# M3 阶段：浅层 LSTM 自编码器的设计原理

- **状态**：活文档。随项目进展与提问持续更新，每次更新在文末「修订记录」追加一条。
- **建立日期**：2026-09-20
- **对应代码**：`src/main/java/com/leejean/m3/`（`LstmAutoEncoder`、`M3Function`、`M3Grid`、
  `WeightedMseLoss`、`M3Scorer`）
- **对应验收项**：V-M3-2（单元与集成测试）、V-M3-3（离线超参数网格）、V-M3-4（误报率）、
  V-M3-5（注入召回）、V-M3-6（性能报告）

本文只记录**已经落到代码里**的设计，以及**已经实测到**的数字。尚未验证的推断一律显式标注。

---

## 1. 这个模型在检测什么，与 M2 的分工

M2 的 pMCOD 是**点异常**检测器：它问「这个点在它所属的窗口里，半径 R 之内有没有至少 k 个邻居」。
它对孤立的离群点敏感，但对「每个点单独看都正常、合起来却不对劲」的情况无能为力——例如温度与湿度
之间的固定关系被打破、日周期发生相移、变化的快慢变了。

M3 补的正是这一层，称为**上下文异常**（contextual anomaly）：判断一**段**序列作为整体是否符合这台
设备的正常形态。两者是互补的通道，不是替代关系。

---

## 2. 核心机制：用重建误差当异常分

这个模型**从不学习「什么是异常」**。它学的是「这台设备正常时长什么样」，然后把「重建得有多差」
当作异常程度。

具体做法是**自编码器**（autoencoder）：把一段序列喂进网络，要求它把**这段序列本身**原样吐出来。
代码里这一点很直白（`LstmAutoEncoder.java:91`）：

```java
INDArray labels = input.dup();   // 自编码器：标签即输入的副本
```

关键在于中间有一道**瓶颈**：编码器把五维输入压成 `hiddenSize` 维的隐状态，解码器再从这个隐状态把
五维重建回来。网络装不下所有细节，只能优先记住反复出现的规律。训练完之后，正常数据重建得准
（误差小），没见过的形态重建得差（误差大），这个误差就是异常分。

### 2.1 网络结构（两层，故称「浅层」）

| 层 | 类型 | 维度 | 激活 | 代码位置 |
| --- | --- | --- | --- | --- |
| 0 | LSTM 编码器 | 5 → hiddenSize | tanh | `LstmAutoEncoder.java:56` |
| 1 | RnnOutputLayer 解码器 | hiddenSize → 5 | 恒等，MSE 损失 | `LstmAutoEncoder.java:65` |

优化器 Adam，学习率 0.01，Xavier 初始化，随机种子固定为 42 以保证可复现
（`LstmAutoEncoder.java:36-37, 51-54`）。

---

## 3. 为什么必须切窗口

LSTM 在原理上可以处理任意长的序列，训练时却不行，有三个硬约束。

**第一，反向传播必须有终点。** 训练 LSTM 用「沿时间反向传播」（backpropagation through time），
梯度要从序列末端一路回传到开头。一台设备一天 8,640 轮、七天 60,480 轮——回传 60,480 步在计算上
不可行，而且会严重梯度消失，早期时间步实际上学不到任何东西。**窗口长度就是这条回传链的长度**。

**第二，一次训练需要多个样本。** 不切窗口的话整条流只是**一个**样本，无从统计规律。切成长度 L 的
窗口后，七天数据变成 60,480 ÷ L 个样本。

**第三，张量是定长的。** 输入张量形状为 `[1, 5, seqLen]`（`LstmAutoEncoder.java:125`），`seqLen`
即窗口长度。注意第一维是 1，**当前实现一次只喂一个窗口**，这一点在第 7 节还会再讨论。

### 3.1 窗口不重叠

窗口攒满即结算并清空缓冲（`M3Function.java:269` 的 `windowBuffer.clear()`），因此窗口之间
**不重叠**，每个窗口都是从零隐状态开始的独立样本。推论是：**模型只能在窗口内部建立时间关联，
跨窗口的规律它看不见。**

---

## 4. 窗口长度同时拨动三个旋钮，方向相反

这是理解 V-M3-3 为什么必须做实证扫描的关键。

**其一，能看多远的上下文。** 窗口 30 轮 = 5 分钟，120 轮 = 20 分钟。**L 决定了「上下文异常」这个
概念的时间尺度上限**：一个持续 15 分钟才显形的异常，在 5 分钟的窗口里可能每一段看起来都正常。

**其二，样本数与 L 成反比。** 训练段总轮数固定（7 天 = 60,480 轮），L 翻倍，样本数减半。样本少了，
模型更容易把训练集背下来而不是学到规律。

**其三，梯度回传链与 L 成正比。** L 越大，单个样本训练越慢，长程梯度越弱。

把三条放在一起：**L 太短**则上下文不足、退化成一个更笨的点异常检测器，与 M2 功能重叠；**L 太长**
则样本变少、训练变慢、长程梯度变弱。网格 {30, 60, 120} 正是在这三者的张力之间扫描——**没有解析解，
只能测**。

### 4.1 隐藏层大小是另一个方向的旋钮

`hiddenSize` 控制瓶颈宽度。**太小**则压缩过狠，正常数据也重建不准，正常与异常的误差都高、区分不开；
**太大**则瓶颈形同虚设，网络可以近似恒等映射，连异常也能重建得很好，同样区分不开。理想宽度刚好
装得下正常模式而装不下异常。

网格取 {40, 60, 90} × {30, 60, 120} 共九种组合交叉扫描，因为两个旋钮相互影响：窗口越长、序列信息
越多，可能需要更宽的隐藏层才装得下。

---

## 5. 数据如何分段，以及为什么这样分

数据在时间上被切成三段，前后相邻、互不重叠（`M3Function.java:247-265`）：

```
  预热（M1 标定）        训练段            早停段         阈值标定段        在线评分
 ├──────────────┼──────────────────┼─────────────┼──────────────┼──────────────→ 时间
   60,480 轮        trainDays 天      esDays 天     threshDays 天     持续
   （M1 用）        做训练净化        不做净化       不做净化
```

**训练段做净化，早停段不做。** 训练集会把含 M2 离群轮的**整个窗口**剔除，因为自编码器必须只学正常
模式；混进异常就会把异常也学会、从而对它重建得很好、再也检不出来。早停段不剔除，因为它要反映模型
在**真实数据分布**上的表现，而真实数据里本来就有少量异常。

**选型判据是早停集误差，不是训练集误差。** 训练集误差随训练单调下降直到过拟合，不能用来选型。
早停集上模型没有做过梯度更新，它衡量的是泛化能力。早停逻辑是：每个 epoch 结束后在早停集上算一次
误差，连续 `patience` 个 epoch 没有改善就停。这既防过拟合，也省时间。

---

## 6. 损失里的掩码：哪些元素不参与

传感器的 Light 通道存在右删失（读数触顶）。这类值不是真实测量，让网络去拟合它等于逼它学习传感器的
缺陷。代码用 DL4J 的标签掩码处理（`LstmAutoEncoder.java:96` 与 `toMaskArray`）：掩码张量与输入同形，
无效元素置 0，**该元素既不贡献梯度也不贡献误差**。

设备 G 还有一条更强的处置：它的 Light 通道输入被**整体归零**（`M3Function.zeroDeviceGLight`）。
交接文档标明这是临时措施，不得扩散到其他设备或通道。

---

## 7. 实测记录

### 7.1 2026-09-20 首次离线试跑（设备 E，hidden=40，window=30）

命令：`syn-m3-grid.sh --devices E --hidden-grid 40 --window-grid 30 --max-epochs 5 --patience 2`，
数据为六月段重放（2022-05-29 至 2022-06-20）。

| 项目 | 实测 |
| --- | --- |
| m1-out 转储 | 1,103,663 行 |
| scores 转储 | 4,023 行；离群名单 1,060 条 |
| 设备 E 跳过预热 | 60,480 轮（= 7 天 × 8,640，与 M1 标定窗口一致） |
| 设备 E 跳过缺失通道 | 207 轮 |
| 设备 E 可用轮 | 92,148 |
| 训练集 | 2,007 窗（剔除 9 窗；2,007 + 9 = 2,016 = 60,480 ÷ 30，与 7 天训练段精确吻合） |
| 早停集 | 576 窗（= 17,280 ÷ 30 = 2 天 × 8,640 ÷ 30，精确吻合） |
| 早停集误差 | 0.179680（**仅 5 个 epoch，远未收敛，不具参考价值**） |
| 训练耗时 | 495.5 秒 / 5 个 epoch ≈ **99 秒每 epoch** |

**容器侧读数**（来自 ND4J 启动日志）：`Cores: [2]`、`Memory: [0.9GB]`、
`Number of threads used for linear algebra: 1`、`OpenMP BLAS: 1`。

**口径自检通过**：训练集与早停集的窗口数与「天数 × 每天轮数 ÷ 窗口长度」精确吻合，说明离线网格的
切分口径与在线算子一致。

---

## 8. 未解决与待验证

1. **正式网格的耗时是否可接受。** 按单点实测外推，全量九组合乘三设备可能需要数十至数百小时——
   **该外推未经验证**，见第 9 节的处置建议。
2. **批大小为 1 的影响。** 当前每次 `fit` 只喂一个窗口，2,007 个窗口即 2,007 次独立的前向反向传播。
   增大批大小会显著提速，但它同时改变优化行为（逐样本梯度变为批内平均梯度），属于影响模型的算法
   改动，需设计会话裁决。
3. **超参数对检测质量的实际影响尚无任何实测。** 早停集误差只衡量重建能力，不等于检测能力。真正的
   判据是 V-M3-4 的误报率与 V-M3-5 的注入召回。
4. **容器只分到 2 核、0.9 GB、单线程 BLAS**，是否为瓶颈未验证。

---

## 9. 修订记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-20 | 建立。记录网络结构、窗口机制、分段与净化、掩码处置，并写入首次离线试跑的实测读数。 |
